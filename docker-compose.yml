version: '3'
services:
  runner:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - './data:/runner'
    environment:
      URL: ${URL}
      TOKEN: ${TOKEN}
