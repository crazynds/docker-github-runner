version: '3'
services:
  runner:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    volumes:
      - './data:/runner'
      - '/var/run/docker.sock:/var/run/docker.sock'
    environment:
      URL: ${URL}
      TOKEN: ${TOKEN}
